box: debian
build:
  steps:
    - arjen/hugo-build@1.25.1:
        version: "0.32.2"
        theme: beautifulhugo
        flags: --buildDrafts=true
deploy:
  steps:
    - uetchy/gh-pages:
        token: $GITHUB_TOKEN
        repo: Anasme/anasme.github.io
        path: public